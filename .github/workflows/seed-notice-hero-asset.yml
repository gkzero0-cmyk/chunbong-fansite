name: Seed Notice hero asset

on:
  push:
    branches: [feature/notice-hero-banner]
    paths:
      - '.github/workflows/seed-notice-hero-asset.yml'

permissions:
  contents: write

jobs:
  seed:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/notice-hero-banner
      - name: Download validated notice banner and commit it
        shell: bash
        run: |
          set -euo pipefail
          META=$(curl -fsS -X POST 'https://api.firestorage.ai/dev/file/shares/XYAoBuLyBA-X/files/01a08cc852cd707ea00431ff06d6837c/download')
          DOWNLOAD_URL=$(META="$META" python3 - <<'PY'
          import json, os
          payload = json.loads(os.environ['META'])
          url = payload.get('downloadUrl')
          if not url:
              raise SystemExit(f'missing downloadUrl: {payload}')
          print(url)
          PY
          )
          mkdir -p assets
          curl -fsSL "$DOWNLOAD_URL" -o assets/notice-hero-banner.jpg
          python3 - <<'PY'
          from pathlib import Path
          data = Path('assets/notice-hero-banner.jpg').read_bytes()
          if len(data) < 100_000 or not data.startswith(b'\xff\xd8') or not data.endswith(b'\xff\xd9'):
              raise SystemExit(f'invalid JPEG bytes: {len(data)}')
          print(f'VALID_NOTICE_BANNER bytes={len(data)}')
          PY
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add assets/notice-hero-banner.jpg
          if git diff --cached --quiet; then
            echo 'Notice banner asset already present.'
            exit 0
          fi
          git commit -m 'Add Notice hero banner image'
          git push origin HEAD:feature/notice-hero-banner
