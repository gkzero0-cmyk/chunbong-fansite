name: Seed Fanart hero asset

on:
  push:
    branches: [feature/fanart-hero-banner]
    paths:
      - '.github/workflows/seed-fanart-hero-asset.yml'

permissions:
  contents: write

jobs:
  seed:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/fanart-hero-banner
      - name: Download validated fanart banner and commit it
        shell: bash
        run: |
          set -euo pipefail
          META=$(curl -fsS -X POST 'https://api.firestorage.ai/dev/file/shares/Q6JuYkXpuRqv/files/01a08be4af7875a1ae6b633cccf9c8a4/download')
          DOWNLOAD_URL=$(META="$META" python3 - <<'PY'
          import json, os
          payload = json.loads(os.environ['META'])
          url = payload.get('downloadUrl')
          if not url:
              raise SystemExit(f'missing downloadUrl: {payload}')
          print(url)
          PY
          )
          curl -fsSL "$DOWNLOAD_URL" -o assets/fanart-hero-banner.jpg
          python3 - <<'PY'
          from pathlib import Path
          data = Path('assets/fanart-hero-banner.jpg').read_bytes()
          if len(data) < 100_000 or not data.startswith(b'\xff\xd8') or not data.endswith(b'\xff\xd9'):
              raise SystemExit(f'invalid JPEG bytes: {len(data)}')
          print(f'VALID_FANART_BANNER bytes={len(data)}')
          PY
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add assets/fanart-hero-banner.jpg
          if git diff --cached --quiet; then
            echo 'Fanart banner asset already present.'
            exit 0
          fi
          git commit -m 'Add Fanart hero banner image'
          git push origin HEAD:feature/fanart-hero-banner
